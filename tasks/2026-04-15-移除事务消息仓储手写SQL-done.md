# 移除事务消息仓储手写 SQL

## 背景

`EzMybatisTransactionalMessageRepository` 中仍存在 `updateBySql` 手写 SQL，不符合存储层统一使用 ez-mybatis 结构化 API 的要求。

## 需求

- 移除 `EzMybatisTransactionalMessageRepository` 中所有手写 SQL。
- 更新逻辑使用 `EzUpdate`。
- 查询逻辑继续使用 `EzQuery`。
- 删除逻辑使用 `EzDelete`。
- 保持多实例派发抢占、成功/失败状态更新、成功消息清理语义不变。

## 任务列表

- [x] 补充禁止手写 SQL 的回归测试。
- [x] 使用 `EzUpdate` 替换状态更新。
- [x] 使用 `EzDelete` 替换成功消息主表清理。
- [x] 调整相关 fake `EzDao` 测试实现。
- [x] 执行相关测试和默认验证。

## 进度

- 2026-04-15 创建任务记录。
- 2026-04-15 已移除 `EzMybatisTransactionalMessageRepository` 中手写 SQL 入口，状态更新改为 `EzUpdate`，成功消息清理改为 `EzDelete`。
- 2026-04-15 已补充风格回归测试并通过相关模块验证。
