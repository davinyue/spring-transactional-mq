# 查询分页改用 page

## 背景

ez-mybatis 查询中存在 `.limit(limit)` 写法，需要统一改为 `.page(1, limit)`。

## 需求

- 仓储查询分页统一使用 `.page(1, limit)`。
- 移除运行时代码中的 `.limit(limit)`。
- 补充回归测试避免再次引入。

## 任务列表

- [x] 定位所有 `.limit(limit)` 使用点。
- [x] 补充禁止仓储模块使用 `.limit(limit)` 的回归测试。
- [x] 将查询分页改为 `.page(1, limit)`。
- [x] 执行相关测试验证。

## 进度

- 2026-04-15 创建任务记录。
- 2026-04-15 已确认仓储模块共有 4 处 `.limit(limit)`。
- 2026-04-15 已全部替换为 `.page(1, limit)` 并通过验证。
