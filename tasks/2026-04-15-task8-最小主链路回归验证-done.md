# Task 8 - 最小主链路回归验证

## 背景

Task 1~7 已完成，当前需要在 starter 层把 `EzDao`、仓储默认实现、`MessagePublishService`、`MessageDispatchService` 和 `RabbitTemplate` 的最小主链路真正串起来，用测试验证自动装配与核心调用链可用。

## 目标

- starter 上下文能够装出主链路 bean
- 调用发布服务后，消息进入 repository 保存逻辑
- 调用派发服务后，消息进入 `MqProducerAdapter` / `RabbitTemplate` 路径
- 不引入真实数据库或真实 RabbitMQ

## 任务列表

- [x] 增加 starter 集成回归测试
- [x] 用最小 fake/mock bean 打通发布与派发链路
- [x] 运行 starter 模块回归测试
- [x] 必要时补一个最小设计缺口修复

## 完成进度

- 100%
