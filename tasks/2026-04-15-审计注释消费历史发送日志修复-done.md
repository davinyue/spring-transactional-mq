# 审计注释消费历史发送日志修复

## 背景

真实环境测试后发现 SQL 注释、消费历史记录和发送日志存在缺口，需要修复表结构脚本和运行时落库行为。

## 需求

- `resources/sql` 下建表脚本需要包含表注释和列注释。
- 消费成功时同时写入 `TXN_CONSUMED_MESSAGE` 和 `TXN_CONSUMED_MESSAGE_HISTORY`。
- 定时清理只需要清理 `TXN_CONSUMED_MESSAGE` 主表，不再移动数据到历史表。
- 消息派发时写入 `TXN_MESSAGE_SEND_LOG`。
- 使用测试验证发送日志、消费主表和消费历史表写入。

## 任务列表

- [x] 补充失败测试。
- [x] 修复发送日志写入。
- [x] 修复消费历史写入与主表清理语义。
- [x] 补充 SQL 表/列注释。
- [x] 执行最小验证和真实环境测试。

## 进度

- 2026-04-15 创建任务记录。
- 2026-04-15 完成发送日志、消费历史、SQL 注释修复。
- 2026-04-15 真实环境测试通过，默认测试通过。
