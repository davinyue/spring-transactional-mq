# README 重写

## 背景

当前 README 存在多处与代码现状不一致的内容，例如 MQ 支持范围、消费者自动注册方式、调度器内置情况和接入示例接口签名等。业务接入方阅读后容易形成错误预期，增加接入成本。

## 目标

- 基于当前代码现状重写 README。
- 以业务接入方视角组织内容，优先回答“是什么、怎么接、怎么发、怎么消费、有哪些限制”。
- 清理与现状不一致或容易误导的说明。

## 范围

- 重写仓库根目录 `README.md`。
- 更新接入依赖、配置项、发送示例、消费示例和当前支持范围说明。
- 补充本次需求记录。

## 非目标

- 本次不调整代码实现。
- 本次不新增额外文档拆分。
- 本次不补充面向贡献者的源码开发手册。

## 关键设计

- README 采用“业务接入优先”结构：简介、适用场景、支持范围、快速接入、发送/消费示例、数据表、验证方式、限制说明。
- 示例以 `spring-transactional-mq-spring-boot-starter` 为主入口，同时明确 RabbitMQ/Kafka 模块需要业务工程显式引入。
- 所有说明以当前代码为准，例如消费者通过 `getSupportMqType()` 声明所属 MQ，starter 已内置派发线程与清理调度。

## 验收标准

- README 中的依赖、配置项、核心接口调用方式与当前代码一致。
- README 结构明显偏业务接入，而不是源码实现导览。
- 不再保留已过时的“未实现 Kafka”“无自动注册消费者”“业务自行调度派发线程”等描述。

## 任务清单

### 分析

- [x] 确认当前 README 与代码现状不一致的关键点
- [x] 明确新 README 面向业务接入方

### 实施

- [x] 重写 `README.md`
- [x] 保持内容与当前代码、模块和配置一致

### 验证

- [x] 执行最小必要校对，确认关键接口名和配置项准确
- [x] 记录文档类改动无自动化校验的事实

### 收尾

- [x] 更新当前进度
- [x] 记录剩余风险与阻塞

## 当前进度

- 已完成根目录 `README.md` 重写，结构调整为业务接入优先。
- 已按当前代码更新 MQ 支持范围、依赖引入方式、配置项、发送示例、消费示例和自动装配说明。
- 已执行最小必要校对，通过代码与 README 关键字段比对确认 `getSupportMqType()`、`transactionalmq.*` 配置项、派发调度器与 `sendWithParent` 等描述准确。

## 风险与阻塞

- 本次为文档改动，未执行自动化测试；验证方式为人工校对 README 与当前代码实现。
- README 依赖当前工作区真实代码，若接口名或自动装配逻辑后续继续变动，文档仍需同步更新。
