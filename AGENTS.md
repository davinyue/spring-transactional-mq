# AGENTS.md

## 目标与优先级

- 以完成当前任务为第一目标，优先做最小必要修改，避免无关重构、扫描和产物。
- 规则冲突时，依次遵循：当前用户的具体要求、本文件、现有项目约定。
- 发现既有实现或需求前提有问题时，应说明事实和原因，并在安全范围内继续推进。

## 工作方式

- 先确认目标、约束和影响范围，再开始实施；除缺少关键前提或存在高风险操作外，默认连续完成后续步骤。
- 优先使用 `rg` 搜索文件和文本；只读取与当前任务直接相关的文件。
- 修改前说明简要思路；完成后说明变更点、影响范围、验证结果及剩余风险或阻塞。
- 保持现有目录、命名、代码风格和工具链，不因个人偏好进行大规模重构。

## 文件与安全

- 所有文本文件使用 UTF-8 和 LF 换行符；发现 CRLF 时统一转换为 LF。
- 使用 `apply_patch` 修改文件；不得通过破坏性命令覆盖或删除未确认范围内的内容。
- 工作区可能存在用户的未提交改动；不得覆盖、回滚或混入与当前任务无关的修改。
- 未经明确要求，不新增示例、演示文件或与任务无关的说明文档。

## Java 规范

- 除 `java.lang` 和同包类型外，使用的 Java 类型必须显式 `import`。
- 新增或修改的类、属性、方法及方法参数必须提供 JavaDoc，并遵循现有代码风格。
- 修改 Java 代码后，必须执行 `python3 ./scripts/check_code_style.py`；检查未通过时，必须修复全部问题后再交付。

## 验证

- 完成修改后，执行与改动直接相关的最小必要测试、构建或检查。
- 优先执行受影响模块的测试；无法执行时，说明原因及可能风险。
- 提交前执行 `git diff --check`，确认没有空白错误、调试内容、临时文件或敏感信息。

## Git 提交

- 未经用户要求，不主动创建 Git 提交。
- 需要提交时，提交信息使用 `<type>: <简短中文描述>`，或 `<type>(<scope>): <简短中文描述>`。
- 每个提交聚焦单一变更点，提交前检查 diff，避免混入无关改动。

## 需求记录

- 代码修改需求必须在 `story/` 目录创建对应需求记录；非代码需求无需创建。
- 进行中的文件命名为 `story/YYYYMMDD-HH-mm-需求主题.md`；验收完成后才可重命名为 `-done.md`。
- 记录至少包含背景、目标、范围、非目标、关键设计、验收标准、任务清单、当前进度和风险阻塞。
